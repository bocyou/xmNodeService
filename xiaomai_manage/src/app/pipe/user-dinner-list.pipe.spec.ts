import { UserDinnerListPipe } from './user-dinner-list.pipe';

describe('UserDinnerListPipe', () => {
  it('create an instance', () => {
    const pipe = new UserDinnerListPipe();
    expect(pipe).toBeTruthy();
  });
});
