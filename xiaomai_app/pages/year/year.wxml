<!-- pages/year/year.wxml -->
<swiper 
indicator-dots="{{true}}" 
autoplay="{{false}}" 
duration="{{360}}"
vertical="{{false}}"
class="box"
>
    <swiper-item>
        <view class="block user">
           <view>
             <view class="user_face bg" style="background-image:url({{user_info.wx_img}});"></view>
             <view class="txt">从2018年4月18日起小麦与您共同走过</view>
             <view class="txt"><text class="numbers">{{long_time}}</text>秒</view>
             <view class="tip"> 左滑查看详细内容</view>
           </view>
            
        </view>
    </swiper-item>
    <swiper-item>
        <view class="block dinner">
            <view>在小麦订餐{{dinner_sum}}次,共补差价{{dinner_all_money}}元。</view>
            <view>最长点</view>
            <view wx:for="{{dinner_list}}" wx:key="{{item}}">{{item.list.name}}-点了{{item.num}}次</view>
            <block wx:if="{{dinner_day0.length>0}}">
             <view >
               <block wx:if="{{dinner_day0.length>=10}}">
               哪有什么加班费，从来只有加班到废！
               </block>
               <block wx:elif="{{dinner_day0.length>1&&dinner_day0.length<5}}">
                 加班等于还未失业，除此之外，我再也找不到其它理由安慰你。
               </block>
                <block wx:elif="{{dinner_day0.length<101&&dinner_day0.length>=5}}">
                 加班等于还未失业，除此之外，我再也找不到其它理由安慰你。
               </block>
             </view>

             <view>在周日点了</view>
            <view wx:for="{{dinner_day0}}" wx:key="{{item}}">
               <view>{{item.create_time}}</view>
               <view wx:for="{{item.dinner_list}}" wx:key="{{item}}">
                 {{item.num}}份{{item.list.name}}
                 
               </view>
            </view>
            </block>
        </view>
    </swiper-item>
    <swiper-item>
        <view class="block">
            <view>
            在小麦刮卡{{draw_count}}次
            <block wx:if="{{draw_count>190}}">
             
            </block>
            <block wx:elif="{{130<draw_count<190}}">
               正常
            </block>
            <block wx:elif="{{draw_count}}"></block>
            </view>
            <view>白吃白喝：{{draw_list[0].num}}</view>
            <view>8元：{{draw_list[7].num}}</view>
        </view>
    </swiper-item>
    <swiper-item>
        <view class="block">
            押注{{bet_list.length}}次 
            <block wx:if="{{bet_win.length>0}}">
                中奖记录
                <view wx:for="{{bet_win}}" wx:key="{{index}}"></view>
            </block>
            <block wx:else>没有中过奖</block>
        </view>
    </swiper-item>
    <swiper-item>
        <view class="block">消费{{shop_data.length}}次，
  共计{{shop_money}}元</view>
    </swiper-item>
     <swiper-item>
        <view class="block">END</view>
    </swiper-item>
</swiper>